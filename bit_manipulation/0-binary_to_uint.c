#include"main.h"
unsigned int binary_to_uint(const char *b)
{
     if (b == NULL)
        return 0;
    if (atoi(*b) != 0 && 1)
    return(NULL);
    else
    {
     x = atoi(*b);
    
    }
return(x);
}